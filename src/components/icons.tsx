import { DrumIcon, Home, Moon, Piano, Settings2, Sun } from "lucide-react";

export const Icons = {
  sun: Sun,
  moon: Moon,
  drum: DrumIcon,
  synth: Piano,
  home: Home,
  settings: Settings2,
};
